query GetChatRoom(
    $roomGuid: String!
    $firstMembers: Int!
    $afterMembers: Int!
) {
    chatRoom(roomGuid: $roomGuid) {
        cursor
        node {
            guid
            roomType
            id
            isChatRequest           
        }
        members(first: $firstMembers, after: $afterMembers) {
            edges {
                cursor
                role
                node {
                    name
                    username
                    id
                    guid
                }
            }
            pageInfo {
                hasNextPage
                hasPreviousPage
                startCursor
                endCursor
            }
        }
    }
}
