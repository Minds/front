mutation StoreFeaturedEntity(
  $entityGuid: String!,
  $autoSubscribe: Boolean,
){
  storeFeaturedEntity(featuredEntity: {
    entityGuid: $entityGuid
    autoSubscribe: $autoSubscribe
  })  {
    tenantId,
    entityGuid,
    autoSubscribe
  }
}
