import { Component } from '@angular/core';

/**
 * Simple wrapper component that isolates presentation
 * of the navigation > navigation menu sub-tab.
 * (in case we choose to add more sub-tabs to the navigation tab)
 */
@Component({
  selector: 'm-networkAdminConsoleNavigation__menu',
  templateUrl: './menu.component.html',
})
export class NetworkAdminConsoleNavigationMenuComponent {}
