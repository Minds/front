export enum MultiTenantRolesView {
  PERMISSIONS = 'permissions',
  USERS = 'users',
}

export enum RoleId {
  OWNER = 0,
  ADMIN = 1,
  MODERATOR = 2,
  VERIFIED = 3,
  DEFAULT = 4,
}

// ojm maybe no need
export enum RoleName {
  OWNER = 'owner',
  ADMIN = 'admin',
  MODERATOR = 'moderator',
  VERIFIED = 'verified',
  DEFAULT = 'default',
}
