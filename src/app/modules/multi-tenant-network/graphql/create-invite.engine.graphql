mutation createInvite(
  $emails: String!
  $bespokeMessage: String!
  $roles: [Int!]
  $groups: [String!]
) {
  invite(
    emails: $emails
    bespokeMessage: $bespokeMessage
    roles: $roles
    groups: $groups
  )
}
