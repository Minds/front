query getInvite($inviteId: Int!) {
  invite(inviteId: $inviteId) {
    inviteId
    email
    status
    bespokeMessage
    createdTimestamp
    sendTimestamp
    id
    roles {
      id
      name
      permissions
    }
    groups
  }
}
