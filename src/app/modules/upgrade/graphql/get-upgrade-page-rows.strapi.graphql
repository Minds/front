query UpgradePage {
  upgradePages(sort: "bulletOrderWithinCard:ASC") {
    data {
      attributes {
        cardId
        rowType
        displayText
        iconId
        iconSource
      }
    }
  }
}
