query UpgradePage {
  upgradePages(sort: "bulletOrderWithinCard:ASC", pagination: { limit: 30 }) {
    data {
      attributes {
        cardId
        rowType
        displayText
        iconId
        iconSource
      }
    }
  }
}
