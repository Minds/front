// options for connect twitter modal
export type ConnectTwitterModalOpts = {
  contextText?: string; // only appears in mobile widths
  titleText?: string;
  bodyText?: string;
  onConnect: () => any;
};
